import { Fira_Mono, Inter } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});

export const fira = Fira_Mono({
  subsets: ['latin'],
  weight: ['400', '700'],
  display: 'swap',
});
